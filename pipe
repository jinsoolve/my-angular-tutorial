loading = false;

this.searchControl.valueChanges.subscribe(query => {
  console.log('입력됨:', JSON.stringify(query));
  this.loading = true;
  this.search(query).subscribe(() => {
    this.loading = false;
    console.log('로딩 끝');
  });
});